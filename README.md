# 智学导评

当前稳定基线：`v0.3`

项目原型阶段：`智学导评 V0.2 → v0.3 closing`

当前仓库：`ai-guided-teaching-assistant`

本地历史目录仍为：`ai-guided-sql-assessment`

GitHub 地址：

```text
https://github.com/myneverlandemmie/ai-guided-teaching-assistant
```

## 0. v0.3 更新说明

v0.3 是在案例材料提交后，对教师端原型进行产品化和工程化收束形成的本地演示基线。相比早期 V0.2，v0.3 的重点不是新增学生端或自动批阅能力，而是提升教师端主流程的稳定性、可维护性和错误提示体验。

本次 v0.3 收束主要包括：

1. **Phase 1：工程结构收束**

   * 完成 `main.py` route 拆分；
   * 将主要业务 route 拆入 `backend/app/routes/`；
   * 完成测试大文件拆分；
   * 将共享测试 helper 改为显式复用；
   * 建立 worklog、QA checklist 和专项 audit 文档体系。

2. **Phase 2：中文友好错误提示与异常处理统一**

   * 收束课次资料上传格式、空表、超限等提示；
   * 补齐授课计划上传超限提示；
   * 统一无 DeepSeek API Key 和 AI 服务不可用时的本地 fallback 提示；
   * 补齐非法 `return_to` 回退提示；
   * 补齐学习通导出、Markdown 下载、DOCX 下载失败提示；
   * 避免向教师展示 traceback、服务器路径、底层异常或敏感信息。

3. **Day 12.1：DOCX 下载 V0.1**

   * 为导学案、任务包和备课参考建议提供基础 `.docx` 下载；
   * 支持标题、普通段落、列表、加粗、行内代码和代码块；
   * DOCX 定位为教师可继续编辑的基础稿，不是精排模板或完整 Markdown 转换器。

4. **当前测试基线**

   * 自动化测试基线为：`174 passed`。

5. **未包含能力**

   * v0.3 仍不包含账号体系、学生端、自动批阅、教师能力评价、完整版本历史、PDF 导出和正式部署能力。这些内容将作为后续版本继续规划。

## 1. 项目定位

智学导评是面向中职教师的 AI 辅助教学设计分析与导学案生成系统。当前工程状态对应 v0.3 稳定基线，重点是把案例材料提交后的教师端流程继续产品化、工程化，并形成可继续迭代的本地演示版本。

系统服务教师围绕课程、授课计划、课次资料、知识主干、课前学情测试、学生导学案和备课参考建议开展教学准备。适用对象包括中职教师、课程负责人、教研组和教学案例建设场景。

本项目不是学生端系统，不是自动备课系统，不是自动批阅平台，也不是教师能力评价系统。所有 AI 输出和本地 fallback 输出均为教师草稿，必须由教师审阅、修改、确认后再使用。

## 2. 当前能力概览

当前 v0.3 基线已支持：

1. 课程管理，默认演示入口为 `/ui-v2/courses`。
2. 授课计划上传与正式课次生成。
3. 课次资料上传 / 粘贴，多文件资料整理和删除。
4. 资料文本提取，支持 `txt`、`md`、`docx`、`pptx`、`xlsx`。
5. 知识主干生成、教师编辑和保存。
6. 课前学情测试生成、题卡预览、单题编辑和删除。
7. 学生导学案、巩固提升任务包和拓展探究任务包生成。
8. 备课参考建议生成。
9. Markdown 下载。
10. DOCX 下载 V0.1。
11. 学习通题库模板导出。
12. DeepSeek API Key 会话级设置、掩码显示、清除和模型选择。
13. 本地 fallback，用于无 Key 或 AI 服务不可用时生成教师可继续编辑的结构化草稿。
14. 中文友好错误提示，覆盖上传、AI fallback、非法返回地址、导出 / 下载失败等核心场景。

旧页面仍保留为 legacy / 兼容页面，用于兼容入口、测试入口和部分回退入口。当前推荐使用 V2 页面演示，不建议在未安排专项迁移前删除旧模板。

## 3. v0.3 已完成重点

v0.3 收束时的工程重点：

1. Phase 1 route 拆分已完成。
2. `backend/app/main.py` 已收束为 FastAPI app 初始化、公共依赖初始化、静态文件和模板挂载、router 注册及必要启动逻辑。
3. 测试拆分已完成，测试 helper 改为显式复用。
4. Phase 2 中文错误提示与异常处理统一已完成当前目标范围。
5. Day 12.1 DOCX 下载 V0.1 已完成。
6. 当前自动化测试基线为 `174 passed`。

## 4. 当前不支持 / 后续计划

当前暂不支持：

1. 暂无账号体系。
2. 暂无学生端。
3. 暂无自动批阅。
4. 暂无完整版本历史。
5. 暂无正式部署能力。
6. DOCX 仍为基础可编辑稿，不是精排模板。
7. PDF 导出可后续再做。
8. 不支持学习通 API 直连或自动发布。
9. 不支持 OCR、图片、扫描件、旧版 `.doc/.ppt`、`xls` 解析。

后续建议按版本推进：

1. `v0.4`：草稿版本保护。
2. `v0.5`：Prompt 校本化与质量校验。
3. `v0.6`：轻量访问控制 / 校内局域网试用。
4. `v0.7`：部署能力。

## 5. 安装前准备

如果主要使用 Windows，推荐用 WSL + Ubuntu 运行本项目。

需要准备：

1. Windows 10 或 Windows 11。
2. WSL2。
3. Ubuntu。
4. Git。
5. Python 3。
6. 浏览器，例如 Edge、Chrome 或 Firefox。

## 6. Windows + WSL 安装步骤

第一步：以管理员身份打开 Windows 的“终端”或“PowerShell”，安装 WSL。

```powershell
wsl --install
```

通常这一步会安装 WSL 和默认 Ubuntu。安装完成后，按提示重启电脑。

如果执行 `wsl --install` 后没有自动安装 Ubuntu，可以打开 Microsoft Store，搜索并安装 Ubuntu。

第二步：打开 Ubuntu。第一次打开时，系统会要求设置 Ubuntu 用户名和密码。这个密码用于 Ubuntu 内部操作，输入时屏幕不显示字符是正常现象。

第三步：更新 Ubuntu 软件列表。

```bash
sudo apt update
```

第四步：安装 Git、Python 和虚拟环境工具。

```bash
sudo apt install -y git python3 python3-venv python3-pip
```

## 7. 获取项目代码

在 Ubuntu 终端中选择一个普通目录存放项目，例如：

```bash
mkdir -p ~/projects
cd ~/projects
```

下载项目代码：

```bash
git clone https://github.com/myneverlandemmie/ai-guided-teaching-assistant
```

进入项目目录：

```bash
cd ai-guided-teaching-assistant
```

后续文档中，`<project-root>` 或“项目根目录”都指这个目录。不要把项目固定放在某个特定的个人路径中。

## 8. 创建虚拟环境并安装依赖

在项目根目录执行：

```bash
python3 -m venv .venv
```

启用虚拟环境：

```bash
source .venv/bin/activate
```

安装依赖：

```bash
pip install -r backend/requirements.txt
```

如果提示找不到 `backend/requirements.txt`，请确认当前终端位置在项目根目录。

## 9. 启动系统

Windows 用户可以在项目根目录双击：

```text
start_v2_courses.bat
```

请在项目根目录运行这个 bat，不要单独复制到桌面运行。

这个脚本会启动 FastAPI 服务，并自动打开 V2 课程中心：

```text
http://127.0.0.1:8000/ui-v2/courses
```

第一次启动可能超过脚本等待时间。如果浏览器先打开失败，请等终端中服务启动完成后刷新页面。

也可以手动启动。在 Ubuntu 终端中进入项目根目录，然后执行：

```bash
cd backend
AI_PROVIDER=deepseek PYTHONPATH=. ../.venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

启动后在浏览器打开：

```text
http://127.0.0.1:8000/ui-v2/courses
```

不要把旧版 `/courses` 作为默认演示入口。

## 10. 基本使用流程

教师可以按下面流程演示或试用：

1. 打开 V2 课程中心。
2. 新建或选择课程。
3. 上传授课计划。
4. 查看系统生成的正式课次。
5. 进入某一正式课次。
6. 上传教案、PPT、实训指导书、补充材料，或粘贴文本。
7. 生成知识主干。
8. 教师审阅、修改并保存知识主干。
9. 查看备课参考建议。
10. 生成课前学情测试。
11. 预览题卡，按需编辑或删除单题。
12. 导出学习通题库模板。
13. 生成学生导学案、巩固提升任务包和拓展探究任务包。
14. 教师审阅、修改、确认。
15. 按需要下载 Markdown 或 DOCX 文件，用于后续整理或发布到既有教学平台。

## 11. DeepSeek API Key 设置

启动系统后，在浏览器打开：

```text
http://127.0.0.1:8000/ai/settings
```

操作步骤：

1. 粘贴自己的 DeepSeek API Key。
2. 选择模型。
3. 保存后返回课程或课次页面继续生成。

模型建议：

1. `deepseek-v4-flash`：适合快速预览和页面流程验证。
2. `deepseek-v4-pro`：适合正式生成、任务包和备课参考建议。

API Key 只保存在当前会话的服务端内存中。清除 Key、服务重启或会话过期后会失效。不要把 API Key 写入文档、日志、`.env` 示例或提交到 Git。

如果缺少 API Key 或 AI 服务暂时不可用，系统可能生成本地结构化草稿。该草稿不是 DeepSeek 的真实生成结果，只用于教师预览结构和继续编辑。

## 12. 清理运行数据

项目根目录提供：

```text
clean_runtime_data.bat
```

请在项目根目录运行这个 bat，不要单独复制到桌面运行。

它会删除本地运行数据库、上传文件和导出文件，但不会删除源码。

具体会清理：

1. `backend/app.db`
2. `data/uploads` 下的运行文件
3. `data/exports` 下的运行文件

清理后会重新创建：

1. `data/uploads`
2. `data/exports/chaoxing`
3. `data/exports/guides`

这是危险操作，只建议在演示前清空环境或重新开始测试时使用。如果已有重要本地数据，不要执行。

## 13. 安全与伦理边界

1. API Key 不入库，不写入 cookie，不应写入日志、文档或提交记录。
2. 本地演示配置不应公网裸奔。
3. AI 输出和本地 fallback 输出都需要教师确认后再使用。
4. 系统不应用于教师能力评价。
5. 系统不自动发布内容给学生。
6. 课前学情测试用于帮助教师了解学习起点，不作为正式考试成绩。
7. 导学案和任务包用于辅助课堂准备，不应直接替代教师对班级情况的判断。
8. 学习通能力仅为题库模板文件导出，不是学习通 API 直连或自动发布。

## 14. 常见问题

### 端口 8000 被占用怎么办？

说明已有其他程序占用了 8000 端口。可以先关闭之前启动的终端窗口，或在终端中按 `Ctrl + C` 停止旧服务，然后重新启动。

### 忘记启动虚拟环境怎么办？

如果提示找不到 `uvicorn`、`pytest` 或依赖包，请进入项目根目录后执行：

```bash
source .venv/bin/activate
```

再重新执行启动命令。

### 页面打不开怎么办？

先确认启动终端没有报错，并且看到服务运行在 `127.0.0.1:8000`。第一次启动可能较慢，浏览器先打开失败时，等终端启动完成后刷新页面。

### API Key 不工作怎么办？

请检查 Key 是否复制完整，DeepSeek 账户是否可用，模型选择是否正确，网络是否可访问 DeepSeek 服务。不要把 Key 提交到 Git。

### 为什么不能上传 xls？

`xls` 是较旧的 Excel 格式。当前资料提取只支持 `xlsx`，不支持 `xls`。建议先用 Excel 或 WPS 另存为 `xlsx` 后再上传。

### 为什么上传 PDF、图片、扫描件暂不支持？

PDF、图片和扫描件通常需要 OCR 或更复杂的解析流程。当前暂不实现这类解析，建议先整理为 `txt`、`md`、`docx`、`pptx` 或 `xlsx`。

### DOCX 下载现在是什么程度？

DOCX 下载 V0.1 是基础可编辑稿，支持标题、普通段落、列表、加粗、行内代码和代码块等常用 Markdown 结构。它不是精排模板，不承诺完整还原复杂 Markdown、表格、图片、脚注、数学公式或复杂样式。

## 15. 测试

在项目根目录执行：

```bash
cd backend && PYTHONPATH=. ../.venv/bin/pytest -q
```

当前 v0.3 closing 预期：

```text
174 passed
```

测试通过只表示当前已实现链路通过自动化检查，不代表暂缓功能已经完成。

## 16. 部署说明

当前推荐先使用本地 WSL 演示，便于教师和教研场景快速验证流程。

正式部署时应单独设计 HTTPS、数据库、备份、日志、权限、API Key 临时存储和数据安全方案。不建议直接把本地演示配置作为生产配置，也不建议将本系统以无认证方式暴露到公网。

## 17. Prompt 文件与校本化调整

系统重要生成提示词位于 `docs/prompts/` 目录。这些文件用于说明或约束知识主干、备课参考建议、课前学情测试、学生导学案等内容的输出风格和结构。

教研团队可根据学校课程、专业方向、教学材料要求和案例申报口径进行二次调整。普通使用者不建议随意修改 prompt 文件，因为修改后可能影响生成内容的稳定性、格式和教学边界。

修改前请先备份原文件。修改后应重新启动服务，并手动验证知识主干、备课参考建议、课前学情测试和学生导学案等生成效果。如果修改后输出异常，可恢复原 prompt 文件。

不要在 prompt 文件中写入 API Key、学生个人隐私、教师个人隐私或真实敏感数据。
